<Page
    x:Class="UniversalConverter.QueuePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UniversalConverter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Fila de Conversão" Style="{StaticResource TitleTextBlockStyle}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10" Margin="0,10,0,10">
            <Button x:Name="StartButton" Content="Iniciar" Click="StartButton_Click"/>
            <Button x:Name="PauseButton" Content="Pausar" Click="PauseButton_Click"/>
            <Button x:Name="ClearButton" Content="Limpar Fila" Click="ClearButton_Click"/>
        </StackPanel>

        <ListView Grid.Row="2" x:Name="QueueListView">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:QueueItem">
                    <Grid Width="400">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{x:Bind FileName}" FontWeight="Bold"/>
                            <TextBlock Text="{x:Bind Status}" FontSize="12"/>
                            <ProgressBar Value="{x:Bind Status, Converter={StaticResource StatusToProgressConverter}}" IsIndeterminate="{x:Bind Status, Converter={StaticResource StatusToIndeterminateConverter}}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>